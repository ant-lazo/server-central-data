server:
  port: ${PORT:${SERVER_PORT:0}}
spring:
  data:
    mongodb:
        uri: mongodb+srv://user:omarlazopassword@clusterbootcamp.28gww.mongodb.net/db_transaction_enterprise?retryWrites=true&w=majority
eureka:
  client:
     fetch-registry: true
     register-with-eureka: true
     service-url:
        default-zone: http://localhost:8761/eureka
  instance:
     instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
cloud:
  discovery:
    enabled: true
  gateway:
    discovery:
      locator:
        enabled: true
        lower-case-service-id: true